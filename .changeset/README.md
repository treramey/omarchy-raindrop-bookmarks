# Changesets

Runtime changes should include a changeset:

```sh
pnpm changeset
```

Select `io.github.treramey.raindrop-bookmarks`, choose the semver impact, and
describe the change in one sentence. Documentation, tests, and internal CI-only
changes do not need one.
